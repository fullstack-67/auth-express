include ../components/head.pug
body(class="container")
    include ../components/nav.pug
    main
        h1 Home
        if user
            article
                h2=user.name
                p=user.email
                em=`${user.isAdmin? "Admin": "Regular"}`